import { EntryTypeSelectorPipe } from './entry-type-selector.pipe';

describe('EntryTypeSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryTypeSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
